package android.widget;

import android.content.Context;

/** {@code android.widget.Button} — a link in {@link CheckBox}'s ancestry. */
public class Button extends TextView {

    public Button() {
    }

    public Button(Context context) {
        super(context);
    }
}
